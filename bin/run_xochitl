#!/bin/sh

set -eux

# Start the VM
run_vm -serial null -daemonize

# Make sure it's up
wait_ssh
in_vm systemctl start rm2fb

lighttpd -f /opt/lighttpd.conf

# Start xochitl
in_vm LD_PRELOAD=/opt/lib/librm2fb_client.so /usr/bin/xochitl
